# Gallery

## Context

```
"context": {
    "styles": "",
    "modifier": ""
}
```

### context.styles

SCSS Modules styles. You can pass extra SCSS Modules styles when this is added to a component.

### context.modifier

This is a class name or a utility class, generated via `src/styles/app.scss` and loaded globally.
